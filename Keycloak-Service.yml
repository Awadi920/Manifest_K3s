---
apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: default
spec:
  selector:
    app: keycloak
  ports:
  - protocol: TCP
    port: 8085  # Matches containerPort
    targetPort: 8085
  type: ClusterIP
