---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: default
spec:
  selector:
    app: gateway
  ports:
  - protocol: TCP
    port: ${GATEWAY_SERVER_PORT}
    targetPort: ${GATEWAY_SERVER_PORT}
  type: ClusterIP
